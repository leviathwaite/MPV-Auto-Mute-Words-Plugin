# Python dependencies for MPV-Auto-Mute-Words-Plugin
# Install with:  pip install -r requirements.txt
#
# WhisperX requires PyTorch.  For GPU support install the CUDA-enabled wheel
# from https://pytorch.org/get-started/locally/ BEFORE running pip install.

whisperx>=3.1.0
torch>=2.0.0
